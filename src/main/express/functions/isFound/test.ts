// isFound Function Test
//import { isFound } from '.';

//TestCase#1
export const isFoundTC1 = async () => {
    try {
        //mock data
        
    } catch ( error: any) {
        console.log(`[isFound] TestCase#1 Error: ${error}`);
    }
}