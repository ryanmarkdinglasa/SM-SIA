//import { generateCode } from '.'

export const test_generateCode = async () => {
    try {
        console.log('\nFUNCTION TEST [generateCode]: STARTING ... \n');
        console.log('.');
        console.log('.');
        let count = 0;

        // Write test cases here
        
        // result
        console.log('\nFUNCTION TEST: generateCode');
        console.log(`PASSED: [${count}/3]`);

    } catch (error: any) {
        console.log(`FUNCTION TEST: generateCode Error : ${error}`);
    }
}