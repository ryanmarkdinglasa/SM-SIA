import { SFC } from "../../../../system/types";
import * as S from './Styles';

export const AppLayout: SFC = ({className}) => {

    return (
        <>
            <S.Container className={className}>
                
            </S.Container>
        </>
    );
} 